{
  "name": "bleaktheology.com",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf _site && buildawesome && pagefind --site _site && node scripts/verify-build.mjs",
    "serve": "buildawesome --serve --incremental",
    "import": "ruby scripts/import-archive.rb"
  },
  "devDependencies": {
    "@awesome.me/buildawesome": "4.0.0-alpha.10",
    "js-yaml": "5.2.3",
    "pagefind": "1.5.2"
  },
  "allowScripts": {
    "fsevents@2.3.3": true
  }
}
